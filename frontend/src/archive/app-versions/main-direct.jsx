import React from 'react';
import ReactDOM from 'react-dom/client';
import AppDirect from './AppDirect.js';

const root = ReactDOM.createRoot(
  document.getElementById('root')
);

root.render(
  <React.StrictMode>
    <AppDirect />
  </React.StrictMode>
);