import React from 'react';
import EnhancedMessagesPage from '../EnhancedMessagesPage';

// Enhanced Messages Page with conversation management
const MessagesPage = (props) => {
  // Use the enhanced messages page with all the new features
  return <EnhancedMessagesPage {...props} />;
};

export default MessagesPage;